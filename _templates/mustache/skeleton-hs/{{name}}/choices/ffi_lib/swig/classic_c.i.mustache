%module classic_c

%{
#include <stdlib.h>
#include "intro_c/classic.h"
%}

%include "intro_c/classic.h"
//extern unsigned long fact_i(const unsigned int n);
//extern unsigned long fact_lp(const unsigned int n);

//extern float expt_i(const float b, const float n);
//extern float expt_lp(const float b, const float n);

// swig -???? -v -I$(PREFIX)/include -outdir build -o build/classic_c_wrap.c classic_c.i
